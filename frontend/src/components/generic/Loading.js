import Styles from 'components/generic/Loading.module.scss';

const Loading = () => {
    // #region Struktura komponentu (JSX)
    return(
        <div id = {Styles.loading} />
    );
    // #endregion
}

export default Loading;